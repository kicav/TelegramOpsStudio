; M8 placeholder. Do not invoke until the standalone Windows artifact passes smoke tests.
; The final installer will package the complete standalone directory, not source/session/debug files.
#define AppName "TelegramWorkflow"
#define AppVersion "0.1.0-dev0"
